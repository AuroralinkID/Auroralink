import 'package:flutter/material.dart';

class JasaSysadmin extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Jasa Sysadmin"),
      ),
    );
  }
}