import 'package:flutter/material.dart';

class Jasa extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Page Jasa"),
      ),
    );
  }
}